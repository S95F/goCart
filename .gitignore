# Compiled binary
gocart
gocart.exe

# Claude Code worktrees / scratch
.claude/

# Large model assets — served by the static handler, kept out of git.
# Place goCart_{high,med,low}.glb under static/models/ at deploy time
# (or set up git-lfs).
static/models/*.glb
